import time

# Number of iterations
num_iterations = 10

for i in range(num_iterations):
    # Your code here
    # This is just an example
    print("\rIteration:", i+1, end='', flush=True)
    time.sleep(1)  # Simulate some work
